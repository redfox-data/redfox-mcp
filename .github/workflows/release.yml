# RedFox MCP — 双通道发布流水线
#
# 触发条件：推送 v* 标签（如 v0.2.2）
# 流程：PyPI 发布 → 多平台编译二进制 → npm 发布
#
# 所需 GitHub Secrets：
#   UV_PUBLISH_TOKEN  — PyPI API token（pypi-...）
#   NPM_TOKEN         — npm publish token

name: Release

on:
  push:
    tags: ['v*']

jobs:
  # ─────────────────────────────────────────────
  # 1. 发布所有 Python 包到 PyPI
  # ─────────────────────────────────────────────
  pypi-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Build all packages
        run: uv build --all-packages

      - name: Publish to PyPI
        run: uv publish dist/*
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.UV_PUBLISH_TOKEN }}

  # ─────────────────────────────────────────────
  # 2. 多平台编译 PyInstaller 二进制
  #    不依赖 pypi-publish 成功（版本可能已手动发布）
  # ─────────────────────────────────────────────
  build-binaries:
    needs: pypi-publish
    if: always()
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            platform: darwin-arm64
          - os: macos-13
            platform: darwin-x64
          - os: ubuntu-latest
            platform: linux-x64
          - os: windows-latest
            platform: win32-x64

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install PyInstaller
        run: pip install pyinstaller

      - name: Install RedFox MCP packages
        run: |
          pip install \
            "redfox-mcp-core>=0.1.3" \
            "redfox-douyin-mcp>=0.2.1" \
            "redfox-xiaohongshu-mcp>=0.2.1" \
            "redfox-wechat-mcp>=0.2.1" \
            "redfox-bilibili-mcp>=0.1.2" \
            "redfox-toutiao-mcp>=0.1.2" \
            "redfox-tiktok-mcp>=0.1.2" \
            "redfox-ai-search-mcp>=0.1.2" \
            "redfox-ai-gen-mcp>=0.1.1" \
            "redfox-twitter-mcp>=0.1.1" \
            "redfox-youtube-mcp>=0.1.1" \
            "redfox-instagram-mcp>=0.1.1" \
            "redfox-kuaishou-mcp>=0.1.1" \
            "redfox-auto-mcp>=0.1.1" \
            "redfox-wechat-channels-mcp>=0.1.1" \
            "redfox-tools-mcp>=0.1.1" \
            "redfox-mcp>=0.4.1"

      - name: Build binaries
        run: bash npm/build-binaries.sh --platform ${{ matrix.platform }}

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.platform }}
          path: npm/*/bin/${{ matrix.platform }}/
          retention-days: 7

  # ─────────────────────────────────────────────
  # 3. 组装 npm 包并发布
  # ─────────────────────────────────────────────
  npm-publish:
    needs: build-binaries
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Download all binaries
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Assemble npm packages
        run: |
          for pkg_dir in npm/redfox-*-mcp; do
            pkg_name=$(basename "$pkg_dir")
            echo "Assembling $pkg_name ..."

            for platform in darwin-arm64 darwin-x64 linux-x64 win32-x64; do
              src="artifacts/binaries-${platform}/${pkg_name}/bin/${platform}"
              dst="${pkg_dir}/bin/${platform}"
              if [[ -d "$src" ]]; then
                mkdir -p "$dst"
                cp "$src"/* "$dst/"
                chmod +x "$dst"/* 2>/dev/null || true
                echo "  ✓ $platform"
              else
                echo "  - $platform (not found)"
              fi
            done
          done

      - name: Publish to npm
        run: |
          for pkg_dir in npm/redfox-*-mcp; do
            pkg_name=$(basename "$pkg_dir")
            echo "Publishing $pkg_name ..."
            cd "$pkg_dir"
            npm publish --access public || echo "⚠ Failed: $pkg_name (may already exist)"
            cd ../..
          done
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
