[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "redfox-toutiao-mcp"
version = "0.1.1"
description = "RedFoxHub Toutiao MCP server: realtime content search and work detail"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
keywords = ["mcp", "redfox", "toutiao"]
dependencies = [
    "redfox-mcp-core>=0.1.0",
]

[project.urls]
Homepage = "https://github.com/redfox-data/redfox-mcp"
Website = "https://redfox.hk/apis/?source=mcp"

[project.scripts]
redfox-toutiao-mcp = "redfox_toutiao_mcp.server:main"

[tool.uv.sources]
redfox-mcp-core = { workspace = true }

[tool.hatch.build.targets.wheel]
packages = ["src/redfox_toutiao_mcp"]

[tool.hatch.build.targets.sdist]
include = ["src/redfox_toutiao_mcp", "README.md"]
